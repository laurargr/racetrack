{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for racetrack front",
  "main": "index.js",
  "scripts": {
    "start": "cross-env APP_ENV=production NODE_ENV=production node index.js",
    "dev": "cross-env APP_ENV=development NODE_ENV=development node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "laura ribeiro",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
